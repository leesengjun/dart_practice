# 브랜치(branch)
1. 브랜치는 작업공간을 따로 만드는 기능이다.
+ main --> 완성본
+ feature-login --> 로그인 기능작업
+ feature-chat --> 채팅기능 작업

여러사람이 동시에 작업해도 충돌을 줄일 수 있다.
# 브랜치 생성 
현재 프로젝트에서 새 작업 공간 만들기.

__git checkout -b feature-login__
의미: 
+ checkout --> 브랜치로 이동
+ -b 새 브랜치 생성
+ feature-login--> 브랜치 이름

## commit
작업 저장.
git add .
수정 파일 준비
git commit 
저장
-m 
저장 메시지 작성

# push
github에 업로드.
__git push origin feature-login__
의미: 
+ origin --> github 저장소
+ feature-login--> 업로드할 브랜치

# PR
내 코드 확인 후 합쳐주는 것
Github에서 생성한다.
흐름:
1. push
2. github 접속
3. Compare & pull request 클릭
4. 설명작성
5. PR 생성

# merge
브랜치 내용을 main에 합치기.
Github에서 __Merge pull request__ 버튼 누름.
또는 터미널 : 
__git chekout main__
__git merge feature-login__

## 실제 협업 흐름 예시 
1단계 main 최신화   
+ git checkout main   
+ git pull origin main

2단계 브랜치 생성      
+ git checkout -b feature-login  

3단계 코드 작성 후 저장   
+ git add .   
+ git commit -m "로그인 UI 구현"   

4단계 GitHub 업로드   
+ git push origin feature-login   

5단계 PR 생성   
+ GitHub에서:   
+ Compare & pull request   
+ 버튼 클릭.   

6단계 merge   
+ Merge pull request   
+ 클릭해서 main에 합침.

# 자주쓰는 명령어
상태확인  
+ git status

브랜치 목록 
+ git branch

브랜치 생성 + 이동
+ git checkout -b 브랜치명

파일 저장 준비
+ git add .

GitHub에 업로드
+ git push origin 브랜치명

최신코드 가져오기
+ git pull origin mian

merge
+ git merge 브랜치명
